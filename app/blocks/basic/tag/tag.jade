mixin tag(text,type)
	span.tag(class= type ? 'tag--red js-add-tag' : '')&attributes(attributes).js-tag
		if type
			i.icon-plus-11.tag-plus-icon
		.tag__select
			select
				option Производитель
				option Комплект
		| !{text}
		if !type
			i.icon-close-12.tag-close-icon.js-remove-tag

